import React from 'react';

const WeatherGraphic = (props) => {

    // VARS
    let weatherType; 

    
    //
    return (
        <img src={weatherType} />
    );

};

export default WeatherGraphic;

